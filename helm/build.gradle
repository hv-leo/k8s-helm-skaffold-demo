description = "k8s-demo helm chart"

task helmChartPackage(type:Exec) {
    workingDir "."
    commandLine "helm", "package", "chart", "-d", "${buildDir}", 
                "--app-version", "${version}", "--version", "${version}"
}